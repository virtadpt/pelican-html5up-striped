{% if DEFAULT_PAGINATION %}
<!-- Pagination -->
<!-- Some of this is cribbed from the default Pelican Simple theme -->

{% set first_page = articles_paginator.page(1) %}
{% set last_page = articles_paginator.page(articles_paginator.num_pages) %}

<div class="pagination">

    <div class="pages">

    {% if articles_page.has_previous() %}
        <a href="{{ SITEURL }}/{{ articles_previous_page.url }}">&laquo;</a>
    {% endif %}

    {% if articles_page.has_next() %}
        <a href="{{ SITEURL }}/{{ articles_next_page.url }}">&raquo;</a>
        <a href="{{ SITEURL }}/{{ last_page.url }}">&#8649;</a>
    {% endif %}


    {% for i in range(1, articles_paginator.num_pages) %}
        {% if i == articles_page.number %}
        <a href="#" class="active">{{ i }}</a>
        {% else %}
        <a href="{{ SITEURL }}/{{ articles_paginator.page(i).url }}">{{ i }}</a>
        {% endif %}
    {% endfor %}



    ...


    <!--
    <a href="{{ SITEURL }}/{{ articles_paginator.page(articles_paginator.num_pages).url }}">{{ articles_paginator.num_pages }}</a>
    </div>
    -->


    <!--
    {% if articles_page.has_next() %}
        <a href="{{ SITEURL }}/{{ articles_next_page.url }}" class="button next">Next</a>
    {% endif %}
    -->



</div>
{% endif %}
